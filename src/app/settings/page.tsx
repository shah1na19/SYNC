import { PageHeader } from "@/ui/layout/PageHeader";

export default function SettingsPage() {
  return (
    <PageHeader
      title="Settings"
      description="Workspace preferences will live here. This route currently demonstrates the application shell."
    />
  );
}
